import { RowColourDirective } from './row-colour.directive';

describe('RowColourDirective', () => {
  it('should create an instance', () => {
    const directive = new RowColourDirective();
    expect(directive).toBeTruthy();
  });
});
